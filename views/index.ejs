<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content = "width=device-width, initial-scale=1.0">
    <title>CHeckList Dashboard</title>

    <style>
        body {
            background-color: rgba(114, 112, 112, 0.373);
        }
        h1 {
            color:brown;
            font-size: 28px;
        }

        table {
            width: 90%;
            margin-top: 20px;
            box-shadow: 5px 3px 3px rgb(72, 72, 72);
        }
        th, td {
            border: 1px solid #302f2f;
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: rgb(114, 113, 113);
            color: #ffffff;
            font-weight: bold;
            
        }

        .passed {
            color:green
        }
        .failed {
            color:red
        }

    </style>
</head>

<body>
    <h1>CheckList Dashboard</h1>
    <table>
        <thead>
            <tr>
               <th>Rule</th>
               <th>Status</th>
            </tr>

        </thead>
        <tbody>
            <% outputResults.forEach(result => { %>
                <tr>
                    <td><%= result.name %></td>
                    <td class="<%= result.currentStatus.toLowerCase() %>"><%= result.currentStatus %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>

</body>

</html>